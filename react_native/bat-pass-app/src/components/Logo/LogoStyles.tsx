import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    textAlign: "center",
    fontSize: 24,
    fontWeight: "bold",
    color: "#e5bf3c"
  },
  logo: {
    resizeMode: "contain",
    height: 180
  }
});