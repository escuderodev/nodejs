import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  logo: {
    resizeMode: "contain",
    height: 180,
    marginBottom: 24,
    marginTop: 16
  },
  title: {
    color: '#e5bf3c',
    fontSize: 32,
    fontWeight: 'bold'
  }
});